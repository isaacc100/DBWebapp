{
  "name": "dbwebapp-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker API for DBWebapp",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "@cloudflare/vitest-pool-workers": "^0.1.0",
    "typescript": "^5.3.3",
    "vitest": "^1.2.2",
    "wrangler": "^3.28.0"
  }
}
